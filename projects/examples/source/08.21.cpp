////////////////////////////////////////////////////////////////////

// chapter : Applied Computations

////////////////////////////////////////////////////////////////////

// content : Numerical Methods
//
// content : Finite Difference Method
//
// content : Derivatives

////////////////////////////////////////////////////////////////////

#include <cassert>
#include <cmath>
#include <numbers>

////////////////////////////////////////////////////////////////////

template < typename T > auto derivative(auto && f, T x, T dx = 1e-9)
{
    auto a = f(x + 1 * dx) - f(x - 1 * dx);

    auto b = f(x + 2 * dx) - f(x - 2 * dx);

    auto c = f(x + 3 * dx) - f(x - 3 * dx);

    return (15 * a / 2 - 6 * b / 4 + 1 * c / 6) / (10 * dx);
}

////////////////////////////////////////////////////////////////////

auto calculate(double x)
{
    return std::sin(x);
}

////////////////////////////////////////////////////////////////////

auto equal(double x, double y, double epsilon = 1e-6)
{
	return std::abs(x - y) < epsilon;
}

////////////////////////////////////////////////////////////////////

int main()
{
    assert(equal(derivative(calculate, std::numbers::pi / 2), 0));
}

////////////////////////////////////////////////////////////////////