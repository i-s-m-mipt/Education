//////////////////////////////////////////////////////////////////

#include <cstdint>
#include <iostream>

//////////////////////////////////////////////////////////////////

enum class State_v1 : std::uint8_t 
{ 
	slow = 0, fast = 1, size 
};

//////////////////////////////////////////////////////////////////

// enum State_v2 : std::uint8_t // bad
// { 
//     slow = 0, fast = 1, size
// }; 

//////////////////////////////////////////////////////////////////

int main()
{
	auto x = 0;

//  --------------------------------------------------------------
	
	std::cout << "main : enter int x : "; std::cin >> x;

	std::cout << "main : selection ";

//  --------------------------------------------------------------

	if (x > 0 && x < static_cast < int > (State_v1::size)) 
	{
		switch (State_v1(x))
		{
			case State_v1::slow : { std::cout << "(1)\n"; break; }
				
			case State_v1::fast : { std::cout << "(2)\n"; break; }

		//  ------------------------------------------------------

			default :
			{
				std::cout << "(3)\n";

			//  ---------------------
				
				break;
			}
		}
	}
}

//////////////////////////////////////////////////////////////////