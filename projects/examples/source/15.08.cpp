////////////////////////////////////////////////////////////////////////////

// chapter : Distributed Network Systems

////////////////////////////////////////////////////////////////////////////

// content : Synchronous Server
//
// content : Sockets
//
// content : System, Registered and Dynamic Ports
//
// content : Tool lsof

////////////////////////////////////////////////////////////////////////////

// support : sudo lsof -i :80
//
// support : sudo ./15.07

////////////////////////////////////////////////////////////////////////////

// support : https://en.wikipedia.org/wiki/List_of_TCP_and_UDP_port_numbers

////////////////////////////////////////////////////////////////////////////

#include <array>

////////////////////////////////////////////////////////////////////////////

#include <boost/asio.hpp>

////////////////////////////////////////////////////////////////////////////

int main()
{
    boost::asio::io_context context;

//  ------------------------------------------------------------------------

    boost::asio::ip::tcp::endpoint endpoint(boost::asio::ip::tcp::v4(), 80);

//  ------------------------------------------------------------------------

    boost::asio::ip::tcp::acceptor acceptor(context, endpoint, true);

//  ------------------------------------------------------------------------

    boost::asio::ip::tcp::socket socket(context);

//  ------------------------------------------------------------------------

    acceptor.accept(socket);

//  ------------------------------------------------------------------------

    std::array < char, 1 << 10 > array = {};

//  ------------------------------------------------------------------------

    boost::asio::read (socket, boost::asio::buffer(array));

    boost::asio::write(socket, boost::asio::buffer(array));
}

////////////////////////////////////////////////////////////////////////////