///////////////////////////////////////////////////////////////

// chapter : Algorithms and Ranges

///////////////////////////////////////////////////////////////

// section : Lambda Expressions

///////////////////////////////////////////////////////////////

// content : Passing Lambda Expressions as Arguments

///////////////////////////////////////////////////////////////

#include <algorithm>
#include <cassert>
#include <set>
#include <vector>

///////////////////////////////////////////////////////////////

int main()
{
	std::vector < int > vector = { 5, 4, 3, 2, 1 };

//  -----------------------------------------------------------

	auto lambda = [](auto lhs, auto rhs){ return lhs < rhs; };

//  -----------------------------------------------------------

	std::ranges::sort(vector, lambda);

//  -----------------------------------------------------------

	assert(std::ranges::is_sorted(vector, lambda));

//  -----------------------------------------------------------

	std::set < int, decltype(lambda) > set = { 5, 4, 3, 2, 1 };

//  -----------------------------------------------------------

	assert(std::ranges::is_sorted(set, lambda));
}

///////////////////////////////////////////////////////////////